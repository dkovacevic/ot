server:
  applicationConnectors:
    - type: http
      port: 8080
  adminConnectors:
    - type: http
      port: 8081
#  requestLog:
#    appenders: []

auth: your_token   # obtained from DevBot when running: show bot <name>
cryptoDir: ./data
